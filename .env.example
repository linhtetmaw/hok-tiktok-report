# Copy to .env for local dev. For Vercel, set these in Project → Settings → Environment Variables.
TIKTOK_SHEET_ID=your_google_sheet_id
TIKTOK_WORKSHEET_NAME=
GOOGLE_APPLICATION_CREDENTIALS=credentials.json
# On Vercel use GOOGLE_APPLICATION_CREDENTIALS_JSON with the full JSON content of credentials.json
