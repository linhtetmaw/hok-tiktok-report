# Minimal deps for Vercel serverless (api/data.py). Must stay under 249 MB.
# Local: use requirements-streamlit.txt for Streamlit, api/requirements.txt for FastAPI.
gspread>=6.0,<7.0
google-auth>=2.28,<3.0
